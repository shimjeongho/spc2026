from dotenv import load_dotenv

from langchain_anthropic import ChatAnthropic
from langchain_core.prompts import PromptTemplate

load_dotenv()

llm = ChatAnthropic(model="claude-sonnet-4-6")

template = PromptTemplate.from_template("다음 주제에 대해 설명해주세요:{topic}\n\n답변에 이모지 포함 금지. 글 문체를 AI티 안나게 사람이 쓴것처럼 최대한 표현해줄것")

formatted_prompt = template.format(topic="LLM 기술")
response = llm.invoke(formatted_prompt)
print(response.content)

formatted_prompt = template.format(topic="Transformer 기술")
response = llm.invoke("인공지능에 대해서 설명해주세요.")
print(response.content)

"""
# LLM 기술이란 무엇인가

## 기본 개념부터 시작하자

LLM은 Large Language Model의 약자인데, 말 그대로 엄청나게 큰 언어 모델이다. 근데 이게 도대체 뭔 말인지 처음 들으면 감이 잘 안 온다.

쉽게 풀어보자면, 인터넷에 있는 글, 책, 논문, 위키피디아 같은 텍스트 데이터를 수백 테라바이트 단위로 먹여서 학습시킨 AI 모델이라고 보면 된다. 이 모델은 학습 과정에서 언어의 패턴, 문맥, 논리 구조 같은 걸 스스로 파악하게 된다. 누가 일일이 규칙을 알려주는 게 아니라, 그냥 엄청난 양의 데이터를 보면서 자체적으로 언어를 이해하는 능력을 갖추게 되는 것이다.

---

## 핵심 기술 구조

### 트랜스포머 아키텍처

LLM의 근간이 되는 기술은 2017년 구글이 발표한 트랜스포머(Transformer)라는 구조다. 이전에는 RNN이나 LSTM 같은 방식으로 언어를 처리했는데, 이 방식은 문장을 앞에서 뒤로 순서대로 읽어야 했다. 당연히 문장이 길어지면 앞 내용을 잊어버리는 문제가 생겼다.

트랜스포머는 이걸 어텐션(Attention) 메커니즘으로 해결했다. 문장 전체를 한 번에 보면서, 각 단어가 다른 단어들과 어떤 관계를 갖는지 동시에 파악하는 방식이다. 예를 들어 "그가 공원에서 뛰다가 넘어졌다. 그는 무릎이 아팠다"라는 문장에서 두 번째 '그는'이 첫 번째 '그가'와 같은 사람이라는 걸 파악하는 능력이 훨씬 좋아진 거다.

### 파라미터의 규모

LLM에서 '크다'는 게 의미하는 건 주로 파라미터 수다. 파라미터는 모델이 학습하면서 조정하는 수치들인데, 이게 많을수록 더 복잡한 패턴을 표현할 수 있다.

GPT-3만 해도 파라미터가 1750억 개다. GPT-4는 정확한 수치가 공개되지 않았지만 이보다 훨씬 크다고 알려져 있다. 이 숫자가 얼마나 큰 건지 감이 안 온다면, 인간 뇌의 시냅스가 약 100조 개 정도 된다는 걸 참고로 보면 된다. 물론 단순 비교는 의미 없지만, 그냥 규모가 어마어마하다는 얘기다.

---

## 학습 방법

### 사전 학습

처음에는 방대한 텍스트 데이터를 가지고 다음 단어를 예측하는 작업을 반복한다. "오늘 날씨가 매우..."라는 문장 다음에 어떤 단어가 올지 맞추는 걸 수억 번 반복하는 식이다. 이 과정에서 모델은 언어의 통계적 패턴과 세상에 대한 지식을 함께 익힌다.

### 파인튜닝과 RLHF

사전 학습만 된 모델은 그냥 텍스트를 이어 쓰는 기계에 가깝다. 이걸 우리가 원하는 방식으로 동작하게 만들려면 추가 학습이 필요하다.

요즘 많이 쓰는 방식이 RLHF인데, Reinforcement Learning from Human Feedback의 약자다. 사람이 직접 모델의 여러 답변을 보고 어느 게 더 나은지 평가하면, 그 피드백을 바탕으로 모델을 개선하는 방식이다. ChatGPT가 그냥 텍스트 생성기가 아니라 대화 상대처럼 느껴지는 이유가 이 과정 때문이다.

---

## 현재 주요 모델들

지금 시점에서 대표적인 LLM을 꼽자면 이렇다.

OpenAI의 GPT 시리즈가 가장 유명하고, 구글은 Gemini를 내놨다. 메타는 Llama 시리즈를 오픈소스로 공개했는데 이게 꽤 파급력이 컸다. 국내에서는 네이버의 HyperCLOVA, 카카오의 KoGPT 같은 모델도 있다. Anthropic의 Claude도 많이 쓰이고 있고.

각각 성능이나 특성이 다르고, 어떤 용도로 쓰느냐에 따라 더 잘 맞는 모델이 따로 있다.

---

## 한계와 문제점

솔직히 LLM이 대단한 건 맞는데, 단점도 명확하다.

**환각 현상(Hallucination)** 이 가장 큰 문제다. 모델이 자신 있게 틀린 정보를 만들어내는 현상인데, 통계적으로 그럴듯한 텍스트를 생성하는 구조적 특성 때문에 발생한다. 아예 없는 논문을 인용하거나, 잘못된 역사적 사실을 확신하며 말하는 경우가 있다.

**추론 능력의 한계**도 있다. 단순한 논리 문제나 수학 계산에서 실수하는 경우가 생각보다 많다. 겉으로는 그럴듯해 보이는 답변인데 내용이 틀린 경우가 많다.

**최신 정보 부재**도 문제다. 학습 데이터에 컷오프 시점이 있어서 그 이후 정보는 모른다. 물론 요즘은 웹 검색을 결합하는 방식으로 어느 정도 보완하고 있다.

**비용과 환경 문제**도 무시 못 한다. 대형 모델을 학습시키는 데 드는 전력과 비용이 엄청나다. GPT-4 학습에 들어간 비용이 수천만 달러 수준이라는 추정치가 있다.

---

## 앞으로의 방향

지금 연구가 활발하게 이루어지는 분야를 보면, 모델 크기를 무작정 키우는 방향보다는 같은 크기로 더 효율적으로 만드는 쪽으로 흐름이 바뀌고 있다. 멀티모달, 즉 텍스트 외에 이미지, 음성, 영상까지 함께 처리하는 능력도 빠르게 발전하고 있다.

또 RAG(Retrieval-Augmented Generation)처럼 외부 데이터베이스와 결합해서 환각 현상을 줄이려는 시도도 많다. 에이전트 방식, 그러니까 LLM이 직접 도구를 사용하고 여러 단계를 거쳐 복잡한 작업을 수행하게 만드는 것도 요즘 핫한 주제다.

LLM이 완벽한 기술은 아니지만, 지금도 빠르게 발전하고 있고 실생활에 미치는 영향은 이미 상당하다. 이걸 제대로 이해하고 활용하는 게 앞으로 점점 더 중요해질 것이다.
# 인공지능(AI, Artificial Intelligence)이란?

## 📌 정의
인공지능이란 **인간의 지능적 행동을 컴퓨터나 기계가 모방**할 수 있도록 만든 기술입니다.
학습, 추론, 문제 해결, 언어 이해 등 인간의 인지 능력을 기계가 수행하도록 합니다.

---

## 🔍 주요 분류

### 1. 능력 범위에 따른 분류
| 종류 | 설명 |
|------|------|
| **약한 AI (Narrow AI)** | 특정 작업만 수행 (예: 챗봇, 이미지 인식) |
| **강한 AI (General AI)** | 인간처럼 모든 분야에서 사고 가능 (아직 미실현) |
| **초인공지능 (Super AI)** | 인간 지능을 초월하는 AI (이론적 개념) |

---

## 🧠 핵심 기술

- **머신러닝 (Machine Learning)**
  - 데이터를 통해 스스로 학습하는 기술
- **딥러닝 (Deep Learning)**
  - 인간 뇌의 신경망을 모방한 심층 학습
- **자연어처리 (NLP)**
  - 인간의 언어를 이해하고 생성하는 기술
- **컴퓨터 비전**
  - 이미지와 영상을 인식하고 분석하는 기술

---

## 💡 활용 분야

- 🏥 **의료** - 질병 진단, 신약 개발
- 🚗 **자동차** - 자율주행 기술
- 💬 **서비스** - 챗봇, 번역, 추천 시스템
- 🎨 **창작** - 그림, 음악, 글 생성
- 💰 **금융** - 사기 탐지, 투자 분석

---

## ⚠️ 주요 이슈

- 일자리 대체 문제
- 개인정보 및 보안 문제
- AI 윤리 및 편향성 문제
- 저작권 문제

---

## 📈 발전 역사

```
1950년대 → AI 개념 등장 (앨런 튜링)
1980년대 → 전문가 시스템 발전
2010년대 → 딥러닝 혁명
2020년대 → ChatGPT 등 생성형 AI 등장
```

---

인공지능은 현재 **4차 산업혁명의 핵심 기술**로, 우리 생활 곳곳에 빠르게 적용되고 있습니다. 더 궁금한 부분이 있으시면 질문해 주세요! 😊
"""

###################################################
from langchain_core.prompts import ChatPromptTemplate


chat_template = ChatPromptTemplate.from_messages([
    ("system", "당신은 {role} 전문가입니다. 질문에 자세히 답변해주세요"),
    ("human", "다음 개념에 대해서 설명해주세요 : {concept}")
])

chain = chat_template | llm

response = chain.invoke({"role": "인공지능", "concept": "트랜스포머"})
print(response.content)

"""
# 트랜스포머 (Transformer)

## 📌 개요

트랜스포머는 2017년 구글이 발표한 논문 **"Attention Is All You Need"** 에서 소개된 딥러닝 아키텍처로, 현재 자연어처리(NLP) 및 AI 분야의 핵심 기술입니다.

---

## 🔍 등장 배경

### 기존 모델의 한계
```
RNN / LSTM / GRU
├── 순차적 처리 → 병렬화 불가능
├── 장기 의존성(Long-term Dependency) 문제
└── 학습 속도가 느림
```
→ 이러한 문제를 해결하기 위해 **트랜스포머** 등장

---

## 🏗️ 전체 구조

```
입력 문장
    ↓
[Input Embedding + Positional Encoding]
    ↓
┌─────────────────────┐
│     Encoder         │  ← N번 반복
│  - Multi-Head       │
│    Self-Attention   │
│  - Feed Forward     │
└─────────────────────┘
    ↓
┌─────────────────────┐
│     Decoder         │  ← N번 반복
│  - Masked           │
│    Self-Attention   │
│  - Cross-Attention  │
│  - Feed Forward     │
└─────────────────────┘
    ↓
출력 문장
```

---

## 🔑 핵심 개념

### 1. 어텐션 메커니즘 (Attention Mechanism)

> "문장에서 어떤 단어가 다른 단어와 얼마나 관련이 있는가?"

```
예시: "나는 사과를 먹었다. 그것은 맛있었다."
→ "그것" = "사과" 에 높은 어텐션 가중치
```

#### 수식
```
Attention(Q, K, V) = softmax(QK^T / √dk) × V

Q (Query)  : 현재 찾고 있는 정보
K (Key)    : 비교 대상 정보
V (Value)  : 실제 전달할 정보
dk         : Key의 차원 수 (스케일링용)
```

---

### 2. 멀티-헤드 어텐션 (Multi-Head Attention)

> 어텐션을 여러 관점에서 동시에 수행

```
입력
 ├── Head 1 → (문법적 관계 파악)
 ├── Head 2 → (의미적 관계 파악)
 ├── Head 3 → (지시어 관계 파악)
 └── Head N → (다양한 패턴 파악)
        ↓
    결과 합치기 (Concat)
        ↓
    Linear Layer
```

---

### 3. 포지셔널 인코딩 (Positional Encoding)

> 트랜스포머는 순서 정보가 없으므로 위치 정보를 별도로 추가

```
"나는 밥을 먹었다"
 나는(1번) 밥을(2번) 먹었다(3번)
      ↓
 각 단어에 위치 정보(벡터)를 더해줌
```

```python
# 포지셔널 인코딩 수식
PE(pos, 2i)   = sin(pos / 10000^(2i/d_model))
PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))
```

---

### 4. 인코더 (Encoder)

```
입력 → [Self-Attention] → [Add & Norm] 
     → [Feed Forward]  → [Add & Norm] → 출력
```
- 입력 문장의 **의미를 이해**하고 표현
- Add & Norm : 잔차 연결(Residual Connection) + 레이어 정규화

---

### 5. 디코더 (Decoder)

```
출력(이전) → [Masked Self-Attention] → [Add & Norm]
           → [Cross-Attention]       → [Add & Norm]
           → [Feed Forward]          → [Add & Norm] → 다음 단어 예측
```
- **Masked Self-Attention** : 미래 단어를 보지 못하게 마스킹
- **Cross-Attention** : 인코더의 출력을 참조

---

## ⚡ 주요 특징 비교

| 특징 | RNN/LSTM | Transformer |
|------|----------|-------------|
| 처리 방식 | 순차적 | 병렬 처리 |
| 장기 의존성 | 약함 | 강함 |
| 학습 속도 | 느림 | 빠름 |
| 메모리 사용 | 적음 | 많음 |
| 확장성 | 제한적 | 뛰어남 |

---

## 🌟 트랜스포머 기반 주요 모델들

```
Transformer (2017)
      │
      ├── BERT (2018) - 구글
      │     └── 문서 분류, 질의응답
      │
      ├── GPT 시리즈 (2018~) - OpenAI
      │     └── GPT-1 → GPT-2 → GPT-3 → GPT-4
      │           └── ChatGPT
      │
      ├── T5 (2019) - 구글
      ├── ViT (2020) - 이미지 처리에 적용
      └── DALL-E, Stable Diffusion - 이미지 생성
```

---

## 📊 활용 분야

| 분야 | 예시 |
|------|------|
| **자연어처리** | 번역, 요약, 챗봇, 감성분석 |
| **이미지** | 이미지 분류, 생성(ViT) |
| **음성** | 음성인식, 합성(Whisper) |
| **코드** | 코드 생성(GitHub Copilot) |
| **멀티모달** | 텍스트+이미지 동시 처리 |

---

## 🎯 핵심 요약

```
트랜스포머 = 어텐션 메커니즘만으로 구성된 모델

✅ 병렬 처리 가능
✅ 장거리 의존성 포착
✅ 확장성이 뛰어남 (더 큰 모델 = 더 좋은 성능)
✅ 다양한 도메인에 적용 가능

→ 현대 AI의 핵심 기반 기술
```

---

궁금한 부분이 있으시면 더 자세히 설명해 드리겠습니다! 😊
"""